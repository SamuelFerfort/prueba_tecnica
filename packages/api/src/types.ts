export type Movimientos = {
    NORTE: { x: number; y: number };
    ESTE: { x: number; y: number };
    SUR: { x: number; y: number };
    OESTE: { x: number; y: number };
  };
export type Direcciones = "NORTE" | "ESTE" | "SUR" | "OESTE";